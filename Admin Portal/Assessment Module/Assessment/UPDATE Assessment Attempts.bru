meta {
  name: UPDATE Assessment Attempts
  type: http
}

patch {
  url: {{BASE_URL}}/assessments/{{assessmentId}}/attempts
  body: json
  auth: inherit
}

body:json {
  {
    "attempts": 5
  }
}

settings {
  encodeUrl: true
  timeout: 0
}