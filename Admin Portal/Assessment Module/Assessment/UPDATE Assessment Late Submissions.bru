meta {
  name: UPDATE Assessment Late Submissions
  type: http
}

patch {
  url: {{BASE_URL}}/assessments/{{assessmentId}}/late-submissions
  body: json
  auth: inherit
}

body:json {
  {
    "lateSubmissions": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}