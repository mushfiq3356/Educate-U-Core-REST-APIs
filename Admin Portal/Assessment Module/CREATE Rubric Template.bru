meta {
  name: CREATE Rubric Template
  type: http
  seq: 7
}

post {
  url: {{BASE_URL}}/assessments/assignment-questions/{{ASSIGNMENT_QUESTION_ID}}/create-template
  body: json
  auth: inherit
}

body:json {
  {
    "templateName": "Sample Rubric Template"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
