meta {
  name: UNASSIGN Lesson Of Course-Module
  type: http
  seq: 9
}

post {
  url: {{BASE_URL}}/course-modules/unassign-lesson
  body: json
  auth: inherit
}

body:json {
  {
    "moduleId": "{{moduleId}}",
    "lessonId": "{{lessonId}}"
  }
}