meta {
  name: UPDATE Assessment Attempts in Course Module
  type: http
}

patch {
  url: {{BASE_URL}}/course-modules/assessment/{{assessmentId}}/attempts
  body: json
  auth: inherit
}

body:json {
  {
    "attempts": 5
  }
}

settings {
  encodeUrl: true
  timeout: 0
}