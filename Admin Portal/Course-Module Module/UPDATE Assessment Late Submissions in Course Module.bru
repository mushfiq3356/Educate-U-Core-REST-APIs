meta {
  name: UPDATE Assessment Late Submissions in Course Module
  type: http
}

patch {
  url: {{BASE_URL}}/course-modules/assessment/{{assessmentId}}/late-submissions
  body: json
  auth: inherit
}

body:json {
  {
    "lateSubmissions": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}