meta {
  name: UPDATE Assessment Attempts in Faculty Course Module
  type: http
}

patch {
  url: {{BASE_URL}}/faculty-course-module/assessment/{{assessmentId}}/attempts
  body: json
  auth: inherit
}

body:json {
  {
    "attempts": 5
  }
}

settings {
  encodeUrl: true
  timeout: 0
}