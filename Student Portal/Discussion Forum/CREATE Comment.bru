meta {
  name: CREATE Comment
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/student-portal/discussion-threads/{{THREAD_ID}}/comments
  body: json
  auth: inherit
}

body:json {
  {
    "comment": "New comments avi",
    "discussionThreadId": "{{THREAD_ID}}"
   // "parentCommentId": "{{PARENT_COMMENT_ID}}" // Optional - for nested comments
  }
}
