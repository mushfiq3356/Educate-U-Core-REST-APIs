meta {
  name: CREATE Comment
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/student-portal/discussion-threads/{{DISCUSSION_THREAD_ID}}/comments
  body: json
  auth: inherit
}

body:json {
  {
    "comment": "This is a sample comment on the discussion thread.",
    "discussionThreadId": "{{DISCUSSION_THREAD_ID}}",
    "parentCommentId": "{{PARENT_COMMENT_ID}}" // Optional - for nested comments
  }
}