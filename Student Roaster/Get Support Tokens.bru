meta {
  name: Get Support Tokens
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/support-tokens?stage=EC2
  body: none
  auth: inherit
}

params:query {
  stage: EC2
  ~scope: own
  ~search: Token
  ~page: 2
  ~pageSize: 20
  ~scope: own
  ~search: sdg
  ~page: 2
  ~pageSize: 20
  ~status: PENDING
}

settings {
  encodeUrl: true
  timeout: 0
}
